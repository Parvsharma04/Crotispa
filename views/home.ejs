<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      :root {
        --primary-color: #6366F1;
        --accent-color: #3f83f8;
        --text-color: #333333;
        --link-color: #2563eb;
        --background-color: #ffffff;
        --light-gray: #f0f0f0;
        --gray: #808080;
        --dark-gray: #555;
}

* {
        margin: 0;
        padding: 0;
        font-family: 'Inter', sans-serif;
        text-decoration: none;
}
      .logo {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        font-weight: bold;
        color: var(--text-color);
        gap: 0.625rem;
}

.logo-icon {
        height: 5.875rem;
        width: 1.938rem;
}

.logo-text {
        font-size: 3rem;
        padding: 2rem;
}

      h1{
        color: #333;
      }

      .search{
        display: flex;
        justify-content: center;
        margin: 2rem;
      }

      #search-bar {
        background-color: #ffffff;
        /* width: 100%; */
        color: #333;
        outline: none;
        border: 1px solid #ccc;
        border-radius: 4px;
        padding: 0.5rem 0.75rem;
      }

      #search-button {
        background-color: #6366f1;
        color: #ffffff;
        border: none;
        border-radius: 0.25rem;
        padding: 0.5rem 1rem;
      }

      .main {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 4rem;
      }

      .container {
        background-color: #6366f1;
        height: 10vh;
        width: 90vw;
        margin: 10px;
        transition: background-color 0.3s ease; /* Add smooth transition effect */
        border: grey 2px solid;
        border-radius: 5px;
        color: white;
        font-size: 1.2rem;
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
      }

      .container:hover {
        background-color: #3f83f8; /* Change color on hover */
      }
      .signup-container {
        background-color: var(--background-color);
        max-width: 1280px;
        height: 50vh;
        margin: 0 auto;
}

.signup-content {
        display: flex;
        flex-direction: row;
        background-color: var(--light-gray);
        height: 100%;
        border-radius: 8px;
        overflow: hidden;
}

.signup-left {
        height: 100%;
        width: 50%;
        display: none;
        position: relative;
}

.signup-left img {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
}

.signup-right {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        padding: 0.5rem;
        justify-content: center;
        align-items: flex-start;
        width: 98%;
}

@media screen and (min-width:640px) {
        .signup-left {
                display: block;
                width: 50%;
        }

        .signup-right {
                width: 50%;
        }

        .signup-container {
                padding: 2rem;
        }
}

.signup-info {
        padding: 0 3rem;
}

.signup-title {
        font-size: 1.5rem;
        font-weight: bold;
        color: var(--primary-color);
        padding-bottom: 1.9rem;
}

.signup-desc {
        text-align: left;
        color: #6366F1;
        padding: 2rem 4rem 1rem 0rem;
        font-size: 1.5rem;
}

.signup-form {
        padding: 0 3rem;
        display: flex;
        flex-wrap: wrap;
        margin-bottom: 1rem;
        gap: 0.6rem;
}

.signup-email {
        background-color: var(--background-color);
        /* width: 100%; */
        color: #333;
        outline: none;
        border: 1px solid #ccc;
        border-radius: 4px;
        padding: 0.5rem 0.75rem;
}

.signup-email:focus {
        border-color: #2563eb;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

.signup-send-button {
        background-color: var(--primary-color);
        color: var(--background-color);
        border: none;
        border-radius: 0.25rem;
        padding: 0.5rem 1rem;
}

.signup-send-button:hover {
        background-color: var(--link-color);
}

.privacy-policy {
        font-size: 0.8jjjjrem;
        padding: 0 3rem;
        color: #777;
}

.signup-link {
        text-decoration: underline;
        color: #2563eb;
}

@media screen and (min-width:640px) {
        .signup-title {
                font-size: 2.5rem;
        }
}
.upload-buttons {
 display: flex;
 flex-direction: row;
 gap: 1rem;
 padding: 0 3rem;
}

.upload-button {
 background-color: var(--primary-color);
 color: var(--background-color);
 border: none;
 border-radius: 0.25rem;
 padding: 0.5rem 1rem;
 cursor: pointer;
 transition: background-color 0.3s ease;
}

.upload-button:hover {
 background-color: var(--link-color);
}

.mail-us{
        display: flex;
        flex-direction: row;
}

    </style>
  </head>
  <body>
    <div class="logo">
      <img src="images/StudySyn.svg" alt="logoImage" class="logo-icon" />
      <span class="logo-text">Hi, <%=username%> Welcome to Crotispa</span>
    </div>
    <!-- <div class="search">
    <input id="search-bar" placeholder="Search previous projects..." />
    <button id="search-button">Search</button>
  </div> -->
    <div class="main">
      <a href="./login.ejs"><div class="container">Join Our New Fintech Of The Future "MONEY TECH" - An All Rounder Money Manager For Your Money</div></a>
      <a href="./AQUA-TECH.html"><div class="container">Join "AQUA TECH" - A company that focuses on genetic mutation of domestic fishes.</div></a>
      <a href="./E-DOLLAR.html"><div class="container">Join "E-DOLLAR" - A start-up that focuses on developing the global currency independent of all countries.</div></a>
      <a href="./BLUE-SPACE.html"><div class="container">Join "BLUE SPACE" - A start-up that aims to expand the knowledge of universe by sending affordable space probes.</div></a>
    </div>

    <div class="signup-container">
      <div class="signup-content">
        <!-- left-part -->
        <div class="signup-left">
          <img src="images/home-page.jpg" alt="signupimg" loading="lazy" />
        </div>
        <!-- right-part -->
        <div class="signup-right">
          <div class="signup-info">
            <h2 class="signup-title">Got An Idea Of The Future?</h2>
            <p>Feel free to discuss your ideas here. If we like it, you will get a chance to host it among our other top ideas.</p>
            <div class="mail-us">
            <p class="signup-desc">Email us your idea at: <a href="mailto:crotispa@gmail.com">crotispa@gmail.com</a></p>
        </div>
          </div>
          <!-- Add this inside the signup-right div, after the signup-form div -->
          <div class="upload-buttons">
        <button class="upload-button" id="upload-pdf">Upload PDF</button>
        <!-- Add these inside the signup-right div, before the upload-buttons div -->
<input type="file" id="pdf-input" style="display: none;" accept=".pdf">

        <button class="upload-button" id="upload-word">Upload Word Document</button>
        <input type="file" id="word-input" style="display: none;" accept=".doc,.docx">
       </div>
       
          <div class="privacy-policy">
            By signing up you agree to our
            <a href="terms.html" class="signup-link">Terms of Service</a>
            <a href="privacy-policy.html" class="signup-link">Privacy Policy</a>
          </div>
        </div>
      </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
          document.getElementById('upload-pdf').addEventListener('click', function() {
            document.getElementById('pdf-input').click();
          });

          document.getElementById('upload-word').addEventListener('click', function() {
            document.getElementById('word-input').click();
          });

          document.getElementById('pdf-input').addEventListener('change', function(e) {
            console.log('PDF file selected:', e.target.files[0]);
            // Handle the uploaded PDF file
          });

          document.getElementById('word-input').addEventListener('change', function(e) {
            console.log('Word document selected:', e.target.files[0]);
            // Handle the uploaded Word document
          });
        });
      </script>
  </body>
</html>